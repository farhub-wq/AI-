# 运行指南

## 1. 目标环境

本文档按以下环境设计：

1. Java 21
2. Maven 3.9+
3. Node.js 20 LTS
4. MySQL 8.4
5. Qdrant
6. 一个支持 Chat + Embedding 的第三方 LLM API

## 2. 推荐运行架构

### 后端

1. Spring Boot 3.3
2. Spring Web
3. Spring Security
4. Spring Data JPA
5. `SseEmitter` 用于流式输出
6. `WebClient` 用于调用第三方 LLM / Embedding API

### 前端

1. Vue 3
2. TypeScript
3. Vite
4. Pinia
5. Vue Router
6. Element Plus

## 3. 第三方 LLM API 接入原则

本项目不绑定本地 Ollama，也不强绑定某个具体厂商，统一采用“OpenAI-compatible 风格配置占位”：

1. `LLM_BASE_URL`
2. `LLM_API_KEY`
3. `LLM_CHAT_MODEL`
4. `EMBEDDING_BASE_URL`
5. `EMBEDDING_API_KEY`
6. `EMBEDDING_MODEL`

这样可以切换到：

1. OpenAI
2. 通义千问兼容接口
3. Kimi 兼容接口
4. DeepSeek 兼容接口
5. 其他兼容网关

## 4. 基础依赖启动

### 4.1 启动 MySQL

```powershell
docker volume create mysql_data
docker run -d `
  --name ai-cs-mysql `
  -e MYSQL_ROOT_PASSWORD=root123456 `
  -e MYSQL_DATABASE=ai_customer_service `
  -e MYSQL_USER=app_user `
  -e MYSQL_PASSWORD=app_password `
  -p 3306:3306 `
  -v mysql_data:/var/lib/mysql `
  mysql:8.4 `
  --character-set-server=utf8mb4 `
  --collation-server=utf8mb4_unicode_ci
```

### 4.2 启动 Qdrant

```powershell
docker volume create qdrant_storage
docker run -d `
  --name ai-cs-qdrant `
  -p 6333:6333 `
  -p 6334:6334 `
  -v qdrant_storage:/qdrant/storage `
  qdrant/qdrant
```

管理面板：

```text
http://localhost:6333/dashboard
```

## 5. 后端启动

### 5.1 推荐目录

```text
backend/
├── src/main/java/com/company/aics/
├── src/main/resources/
├── sql/
├── pom.xml
└── README.md
```

### 5.2 推荐 Maven 依赖

1. `spring-boot-starter-web`
2. `spring-boot-starter-security`
3. `spring-boot-starter-validation`
4. `spring-boot-starter-data-jpa`
5. `spring-boot-starter-webflux`
6. `mysql-connector-j`
7. `jjwt`
8. `springdoc-openapi-starter-webmvc-ui`
9. `pdfbox`
10. `commonmark`
11. Qdrant Java Client
12. `lombok`

### 5.3 环境变量

参考根目录 `.env.example`：

```env
DB_URL=jdbc:mysql://127.0.0.1:3306/ai_customer_service?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
DB_USERNAME=app_user
DB_PASSWORD=app_password

JWT_SECRET=replace-with-a-long-random-secret-at-least-32-chars
JWT_ACCESS_TOKEN_MINUTES=1440

QDRANT_URL=http://127.0.0.1:6333
QDRANT_COLLECTION=kb_chunks

LLM_BASE_URL=https://dashscope-intl.aliyuncs.com/compatible-mode/v1
LLM_API_KEY=replace-with-your-llm-api-key
LLM_CHAT_MODEL=deepseek-r1

EMBEDDING_BASE_URL=https://dashscope-intl.aliyuncs.com/compatible-mode/v1
EMBEDDING_API_KEY=replace-with-your-embedding-api-key
EMBEDDING_MODEL=text-embedding-v3

RAG_TOP_K=12
RAG_SCORE_THRESHOLD=0.22
RAG_MAX_CONTEXT_CHARS=6000
DAILY_QUESTION_LIMIT=100
```

说明：

1. 业务数据走 MySQL（`.env` 中 `DB_*`）；首次空库启动由 `DataSeeder` 写入演示用户与知识库。
2. 向量检索默认走内存索引；Qdrant 可用时自动同步写入。启动种子文档会自动切块并 upsert，可直接问答。
3. Embedding Key 未配置时，会自动回退本地向量，保证无 Embedding API 也能跑通 RAG。
4. Chat 流式接口走 LLM `stream=true`，前端按 SSE `token` 事件逐字渲染。
5. 客服种子文档（约 2000–5000 字）：`backend/src/main/resources/seed/` 下的  
   `公司产品介绍.txt`、`常见问题FAQ.md`、`退换货政策.txt`。
6. 演示登录：`demo@example.com` / `Passw0rd!`（也可手机号 `13800138000`）。

### 5.4 启动命令

```powershell
cd backend
mvn spring-boot:run
```

接口文档：

```text
http://localhost:8080/swagger-ui.html
```

## 6. 前端启动

### 6.1 推荐目录

```text
frontend/
├── src/
├── public/
├── package.json
└── README.md
```

### 6.2 初始化项目

```powershell
npm create vite@latest frontend -- --template vue-ts
cd frontend
npm install
```

### 6.3 推荐依赖

```powershell
npm install vue-router pinia axios dayjs element-plus @vueuse/core
```

### 6.4 前端环境变量

```env
VITE_API_BASE_URL=http://localhost:8080/api/v1
VITE_SSE_BASE_URL=http://localhost:8080/api/v1
```

### 6.5 启动命令

```powershell
cd frontend
npm run dev
```

## 7. 联调顺序

推荐按下面顺序联调：

1. MySQL 是否可连通
2. Qdrant 是否可访问
3. 第三方 Embedding API 是否可调用
4. 第三方 Chat API 是否支持流式返回
5. Spring Boot 健康检查是否通过
6. 文档上传与向量化是否成功
7. SSE 问答接口是否逐字输出
8. Vue 前端是否正确渲染回答与引用
9. Agent 拆解接口是否能输出服务影响面和任务 DAG

## 8. Agent 拆解能力运行建议

如果要让 README 第 6 点真正落地，建议准备以下输入数据：

1. 服务目录文档
2. 接口文档
3. 事件文档
4. 数据库表关系文档
5. 典型需求样例

建议先导入 3-5 个示例服务：

1. 用户服务
2. 订单服务
3. 通知服务
4. 前端商城
5. 风控服务（可选）

## 9. 提交前检查单

1. `.env.example` 中没有真实 API Key
2. 文档中的端口、接口路径、表名和实际代码一致
3. SSE 问答在浏览器可见逐字输出
4. 至少准备 10 条客服问答验证问题
5. 至少准备 3 条跨服务需求拆解验证案例
