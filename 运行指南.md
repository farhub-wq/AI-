# 运行指南

## 1. 目标环境

本文档按以下环境设计：

1. Java 21
2. Maven 3.9+
3. Node.js 20 LTS
4. MySQL 8.4
5. Faiss 本地文件向量索引（无需独立向量服务）
6. 一个支持 Chat + Embedding 的第三方 LLM API

## 2. 推荐运行架构

### 后端

1. Spring Boot 3.3
2. Spring Web
3. Spring Security
4. Spring Data JPA
5. `SseEmitter` 用于流式输出
6. `WebClient` 用于调用第三方 LLM / Embedding API

### 前端

1. Vue 3
2. TypeScript
3. Vite
4. Pinia
5. Vue Router
6. Element Plus

## 3. 第三方 LLM API 接入原则

本项目不绑定本地 Ollama，也不强绑定某个具体厂商，统一采用“OpenAI-compatible 风格配置占位”：

1. `LLM_BASE_URL`
2. `LLM_API_KEY`
3. `LLM_CHAT_MODEL`
4. `EMBEDDING_BASE_URL`
5. `EMBEDDING_API_KEY`
6. `EMBEDDING_MODEL`

这样可以切换到：

1. OpenAI
2. 通义千问兼容接口
3. Kimi 兼容接口
4. DeepSeek 兼容接口
5. 其他兼容网关

## 4. 基础依赖启动

### 4.1 启动 MySQL

```powershell
docker volume create mysql_data
docker run -d `
  --name ai-cs-mysql `
  -e MYSQL_ROOT_PASSWORD=root123456 `
  -e MYSQL_DATABASE=ai_customer_service `
  -e MYSQL_USER=app_user `
  -e MYSQL_PASSWORD=app_password `
  -p 3306:3306 `
  -v mysql_data:/var/lib/mysql `
  mysql:8.4 `
  --character-set-server=utf8mb4 `
  --collation-server=utf8mb4_unicode_ci
```

### 4.2 Faiss 本地文件向量索引（无需 Docker）

本项目按题目 FAQ 选用 **Faiss 本地文件模式**，**不需要**再启动 Chroma / Qdrant 等独立向量服务。

- 配置：`.env` 中 `FAISS_INDEX_DIR`（默认相对 backend 工作目录的 `data/faiss-index`）
- 落盘文件：`data/faiss-index/index.json`（已在 `backend/data/` gitignore 内）
- 启动日志可见：`Faiss local index loaded: N points` 或空索引提示
- 算法：与 Faiss `IndexFlatIP` 同类的精确余弦；同内容 upsert 会复用已落盘向量，减少 Embedding 调用

MySQL 仍建议按上一节启动（业务元数据）；仅向量检索不依赖额外容器。

## 5. 后端启动

### 5.1 推荐目录

```text
backend/
├── src/main/java/com/company/aics/
├── src/main/resources/
├── sql/
├── pom.xml
└── README.md
```

### 5.2 推荐 Maven 依赖

1. `spring-boot-starter-web`
2. `spring-boot-starter-security`
3. `spring-boot-starter-validation`
4. `spring-boot-starter-data-jpa`
5. `spring-boot-starter-webflux`
6. `mysql-connector-j`
7. `jjwt`
8. `springdoc-openapi-starter-webmvc-ui`
9. `pdfbox`
10. `commonmark`（若解析 Markdown）
11. `okhttp`（LLM / Embedding HTTP）
12. `lombok`（可选）

### 5.3 环境变量

参考根目录 `.env.example`：

```env
DB_URL=jdbc:mysql://127.0.0.1:3306/ai_customer_service?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
DB_USERNAME=app_user
DB_PASSWORD=app_password

FAISS_INDEX_DIR=data/faiss-index

LLM_BASE_URL=https://dashscope-intl.aliyuncs.com/compatible-mode/v1
LLM_API_KEY=replace-with-your-llm-api-key
LLM_CHAT_MODEL=qwen-plus

EMBEDDING_BASE_URL=https://dashscope-intl.aliyuncs.com/compatible-mode/v1
EMBEDDING_API_KEY=replace-with-your-embedding-api-key
EMBEDDING_MODEL=text-embedding-v3

RAG_TOP_K=12
RAG_SCORE_THRESHOLD=0.22
RAG_MAX_CONTEXT_CHARS=6000
DAILY_QUESTION_LIMIT=100
```

说明：

1. 业务数据走 MySQL（`.env` 中 `DB_*`）；首次空库启动由 `DataSeeder` 写入演示用户与知识库。
2. **JWT 不在 `.env` 配置**：Access 默认 30 分钟、Refresh 默认 14 天，写在 `application.yml`；HMAC 密钥由 `JwtKeyProvider` 自动生成/读取 `backend/data/jwt.hmac.key`（已 gitignore）。生产将密钥文件挂载到该路径或改 `security.jwt.key-file`。
3. **向量索引**：Faiss 本地文件模式（`FAISS_INDEX_DIR`），无需独立向量服务。启动种子文档会自动切块并 upsert 落盘，可直接问答。
4. Embedding Key 未配置时，会自动回退本地向量，保证无 Embedding API 也能跑通 RAG。
5. Chat 流式接口走 LLM `stream=true`，前端按 SSE `token` 事件逐字渲染。
6. 客服种子文档（约 2000–5000 字）：`backend/src/main/resources/seed/` 下的  
   `公司产品介绍.txt`、`常见问题FAQ.md`、`退换货政策.txt`。
7. 演示登录：`demo@qq.com` / `Passw0rd!`（也可手机号 `13800138000`）。该账号角色为 **ADMIN**，可进入管理后台；自行注册的账号默认为 **USER**，无管理端权限。邮箱注册仅支持 `@qq.com`、`@163.com`、`@gmail.com` 等常见后缀。

### 5.4 启动命令

```powershell
cd backend
mvn spring-boot:run
```

接口文档：

```text
http://localhost:8080/swagger-ui.html
```

## 6. 前端启动

### 6.1 推荐目录

```text
frontend/
├── src/
├── public/
├── package.json
└── README.md
```

### 6.2 初始化项目

```powershell
npm create vite@latest frontend -- --template vue-ts
cd frontend
npm install
```

### 6.3 推荐依赖

```powershell
npm install vue-router pinia axios dayjs element-plus @vueuse/core
```

### 6.4 前端环境变量

```env
VITE_API_BASE_URL=http://localhost:8080/api/v1
VITE_SSE_BASE_URL=http://localhost:8080/api/v1
```

### 6.5 启动命令

```powershell
cd frontend
npm run dev
```

## 7. 联调顺序

推荐按下面顺序联调：

1. MySQL 是否可连通
2. Faiss 本地索引目录是否可写（启动后存在 `data/faiss-index/index.json` 或日志显示已加载点数）
3. 第三方 Embedding API 是否可调用
4. 第三方 Chat API 是否支持流式返回
5. Spring Boot 健康检查是否通过
6. 文档上传与向量化是否成功
7. SSE 问答接口是否逐字输出
8. Vue 前端是否正确渲染回答与引用
9. Agent 拆解接口是否能输出服务影响面和任务 DAG

## 8. 研发变更规划（README 第 6 点，已落地）

### 8.1 已实现内容

| 能力 | 说明 |
| --- | --- |
| 改哪些微服务 | 技术文档检索 + 语义信号启发 + 服务目录打分 → `impactedServices` + `evidenceHits` |
| 哪些可并行 | `executionMode=parallel` 且无 `dependsOn` → `parallelGroups` |
| 哪些必须串行 | 读取 MySQL `service_dependencies` 建边 + 通知依赖订单/用户兜底 |
| 建议发布顺序 | 按任务依赖拓扑导出 `suggestedReleaseOrder` |
| 人工评审 | `reviewChecklist`（影响面确认、依赖复核、回滚点、补文档后再合入等） |
| 后校验 | 强依赖不得进并行组、缺上游提示、依赖环检测 |
| 前端 | 「研发变更规划」工作台：变更单、影响面、依赖、DAG、发布顺序、评审清单 |

代码入口：

- 后端：`AgentPlannerService`、`POST /api/v1/agent/decompose`
- 前端：侧栏「研发变更规划」→ `AgentDecomposeView.vue`

### 8.2 启动后已有的演示数据

空库首次启动时，`DataSeeder` 会写入：

1. **技术文档库**（`technical_docs`）：订单接口、通知事件、用户资料、商城前端结账页  
2. **服务目录**：`order-service` / `user-service` / `notification-service` / `mall-web`  
3. **依赖边**：订单→通知（事件）、用户→通知（数据）、订单→前端（API）等  
4. **示例规划**：默认需求「下单成功后自动发送短信」（含变更单 `CHG-DEMO-001`）

因此**不必再手工导入文档**即可演示第 6 点；后续若要增强准确率，可继续往技术库上传更多接口/事件文档。

### 8.3 推荐验收步骤（约 2 分钟）

1. 登录演示账号，打开侧栏 **研发变更规划**  
2. 保持默认变更单/标题/正文（下单后发短信），服务范围勾选四个服务  
3. 点击 **生成变更规划**  
4. 核对结果大致符合：

| 检查项 | 期望 |
| --- | --- |
| 受影响服务 | 订单、用户、通知、前端（理由中含文档命中或语义信号） |
| 可并行 | 「开放并校验手机号查询」与「更新前端成功状态文案」 |
| 串行依赖 | 通知消费依赖订单事件 + 用户手机号；末尾联调依赖全部任务 |
| 发布顺序 / 评审清单 | 有服务码顺序与人工确认项 |
| 状态 | 有技术证据时多为 `success`；证据不足则为 `partial` 并列出提示 |

接口抽查（可选）：

```http
POST /api/v1/agent/decompose
GET  /api/v1/agent/plans/{planId}
GET  /api/v1/agent/service-catalog
GET  /api/v1/agent/service-dependencies
```

### 8.4 边界（避免误解）

1. 输出的是**改动计划与依赖 DAG**，不是自动改多仓库业务代码（README 也不强制写完整代码）。  
2. 与客服 RAG 隔离：变更规划走技术库与服务目录，不改写客服问答 Prompt。  
3. 更细的设计说明见 `项目说明.md` §4。

